# マニュアル変更点一覧（v2.0.2 → v2.1.0）

実装 `/home/mhaya/weko`（develop_v2.1.0）の v2.0.2 からの差分のうち、**マニュアル（docs/manuals）に反映が必要と考えられる箇所**の一覧。本ファイルは一覧化のみで、マニュアル本文の編集は行っていない（機能仕様書側は反映済み）。

凡例：優先度 ★=要反映（利用者に見える挙動変更） / ▲=条件付き（既定OFFのconfig有効時のみ） / ○=軽微・任意

---

## ユーザ操作マニュアル（docs/manuals/USER/base/README.md）

| # | 章・セクション（目安） | 変更点 | 優先度 | 出典テーマ |
|---|------------------------|--------|--------|-----------|
| U1 | アイテム簡易登録（`## アイテム簡易登録` 5532付近）／ワークスペース（`### ワークスペース` 5384付近） | メタデータ自動補完ソースに **arXiv** を追加。簡易登録の取得元ラジオに「arXiv MetaData」が増える | ★ | D |
| U2 | ワークスペース アイテム一覧の出力（TSV/CSV出力） | 出力ファイルの列見出しがサーバ定義化され、**先頭に「No.」列**が追加。列構成の説明を更新 | ★ | D |
| U3 | ワークスペース アイテム一覧 | 一覧の**DOIリンク表示条件**が「関連情報(relation)の relatedIdentifier のうち identifierType=DOI かつ relationType=isVersionOf」ベースに変更 | ○ | D |
| U4 | コンテンツファイル／ファイル詳細（`#### コンテンツファイル` 1457、`### ファイル詳細画面` 2141付近） | **サイトライセンス利用者**は、open_login/open_restricted のファイルを実ダウンロード可能に。制限公開(open_restricted)ファイルもワンタイムアドレスを介さず直接ダウンロードできる旨を追記 | ★ | F |
| U5 | ファセットで検索する（`### ファセットで検索する` 1255付近） | 「アクセス権(accessRights)」ファセットが、エンバーゴ状態を公開日・現在日で open/restricted/embargoed に振り分けて集計・絞り込み（`WEKO_SEARCH_FIX_ACCESSRIGHTS` 有効時のみ。既定OFF時は従来通り） | ▲ | A |
| U6 | インデックスで検索する／インデックスツリー（`### インデックスで検索する` 707付近） | インデックス閲覧可否が「ロール権限 AND グループ権限」判定に変更。いずれのグループにも属さない利用者・ゲストは「No Group」扱い（表示に影響し得る） | ○ | G |

---

## 管理者マニュアル（docs/manuals/ADMIN/base/README.md）

| # | 章・セクション（目安） | 変更点 | 優先度 | 出典テーマ |
|---|------------------------|--------|--------|-----------|
| A1 | ファイル管理 > Location（`#### Locationを作成する/編集する/削除する` 9873-9940） | **ロケーションの作成/編集/削除はシステム管理者のみ**に変更。リポジトリ管理者は一覧・詳細の**閲覧のみ**（デフォルトロケーションも表示可）。従来「リポジトリ管理者も操作可／デフォルトは非表示」の記述を訂正 | ★ | H(#60332) |
| A2 | ファイル管理 > ファイルインスタンス（`［ファイルインスタンス］` 9810付近） | **ファイルインスタンスの削除操作が可能**に。削除は実体ファイル削除＋ロケーションサイズ再計算を伴う。従来「閲覧のみ」の記述を更新 | ★ | H(#60919) |
| A3 | SWORDAPI設定（`## SWORDAPI設定` 9593付近）／JSON-LDマッピング（`## JSON-LDのマッピングを設定する` 2495付近） | JSON-LD登録でカスタムフラグ **`wk:researchmapLinkage`**（researchmap業績連携フラグ）に対応。ワークフロー経由登録時に連携を実行 | ★ | C |
| A4 | JSON-LDマッピング／インポート（`## JSON-LDのマッピングを設定する` 2495、`### インポートする` 3375付近） | インポート時の**メタデータ文字列置換ルール**（`WEKO_SEARCH_UI_IMPORT_REPLACE_RULES`/`_RULE_MAP`。例：半角`|`→全角`｜`）を追加。マッピングIDごとに適用 | ○ | C |
| A5 | （新規）一括インポートAPI | **OAuth2認証の一括インポートREST API**（`POST /api/items/import-task` ほか）を新設。利用可能ロール＝システム管理者/リポジトリ管理者、スコープ `item:bulkprocess`。API利用手順の記載が必要（該当章が無ければ新設） | ★ | B |
| A6 | OAuth Application（`### OAuth Applicationを管理する` 10602付近） | 発行トークンに新スコープ **`item:bulkprocess`**（一括インポートAPI用）が選択可能に。スコープ不正時は認可画面ではなくエラー画面(invalid_scope)へ | ○ | B/D |
| A7 | OAI-PMH設定（`## OAI-PMH設定` 8798付近） | ハーベスト応答(from/until)がエンバーゴ解除アイテムを公開日到来を境に収集対象へ含める／datestampが公開日基準に繰り上がる（`WEKO_SEARCH_FIX_ACCESSRIGHTS` 有効時のみ。既定OFF時は従来通り） | ▲ | A |
| A8 | Resource Syncの設定（`## Resource Syncの設定` 9108付近） | ChangeList にエンバーゴ解除アイテムが公開日基準で反映される（`WEKO_SEARCH_FIX_ACCESSRIGHTS` 有効時のみ） | ▲ | A |
| A9 | インデックスツリー管理（`## インデックスツリー管理` 4403付近） | インデックスの閲覧/投稿権限判定が「ロール AND グループ」に変更。既定グループに「No Group」を追加。GakuNin mAP ロールは権限選択から除外 | ○ | G |
| A10 | コミュニティ管理（`## コミュニティ管理` 8584付近） | コミュニティ一覧・編集の **Owner 表示名**が変換表示（jc_roles_sysadm→System Administrator 等）。Owner プルダウンから GakuNin mAP ロールを除外 | ○ | G |
| A11 | アイテムタイプ管理 > マッピング（`### アイテムタイプをスキーマにマッピングする` 2407付近） | マッピング保存が upsert（1アイテムタイプ＝1マッピング行）に変更。内部挙動中心のため利用者手順の変更は小。必要なら注記 | ○ | E |

---

## 操作ガイド（docs/manuals/GUIDE/base/manual/*.md）

| # | 対象ファイル | 変更点 | 優先度 | 出典テーマ |
|---|-------------|--------|--------|-----------|
| G1 | researchdatadeposit.md / journalPaperdeposit.md 等 | エンバーゴ／アクセス権に関する説明がある場合、`WEKO_SEARCH_FIX_ACCESSRIGHTS` 有効時の検索・ハーベストでの出し分け挙動を必要に応じ補足（既定OFFのため通常環境では不変） | ▲ | A |
| G2 | （SWORD/GRDM連携ガイドがある場合） | SWORDステータス応答に**ファイルリンク(fileSetFile)**追加・**state に inWorkflow** 追加、GRDM/ワークスペース連携のarXiv対応等 | ○ | C/D |

---

## 反映不要（マニュアル観点）
- DBマイグレーションのベースライン刷新(#61275)、多言語ファイル再コンパイル(#60737)、内部リファクタ、テスト追加は利用者手順に影響しないため反映不要。
- 既定 `WEKO_SEARCH_FIX_ACCESSRIGHTS=False` のため、エンバーゴ関連(▲)は当該configを有効化した環境のマニュアルにのみ関係。

## 補足（実装側の要確認事項）
- `postgresql/ddl/sp72-createindex.sql` が未修正で item_type_mapping の旧btreeインデックス2本＋GINを作る記述のまま。新規構築とマイグレーション後で最終状態が食い違う懸念があり、実装側の確認を推奨（マニュアルではなく構築手順の問題）。
